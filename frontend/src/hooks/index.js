/**
 * Custom React hooks for common UI patterns.
 *
 * These hooks encapsulate reusable state management logic to reduce
 * code duplication and improve component readability.
 */

export { useExpandable } from "./useExpandable";
export { useApiCall } from "./useApiCall";
